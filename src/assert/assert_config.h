// module configuration

// the address where the error code will be stored when panic'ing
#define ASSERT_PANIC_ADDRESS	0xFFFE

// the color to set the border to when panic'ing
// you can use spectrum.h constants here
#define PANIC_COLOUR		INK_MAGENTA
